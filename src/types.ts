/**
 * The props used for styling the components.
 */
export interface IStyleProps {
  /**
   * Required to pass along the styling to enable styled-components
   * styled(Component)`` syntax.
   */
  className?: string;
}
